# Install with: pip install --no-deps for each package group, then let pip resolve at end

# Core packages (install first)
langgraph==0.2.45
langsmith==0.1.143

# LangChain core (compatible set)
langchain-core==0.3.27
langchain==0.3.13
langchain-community==0.3.13
langchain-text-splitters==0.3.4

# Google Generative AI (IMPORTANT: Use these specific versions for compatibility)
langchain-google-genai==2.0.5
google-generativeai==0.8.6
google-ai-generativelanguage==0.6.15

# HuggingFace Embeddings (Local, no API key needed)
# NOTE: Install torch from PyTorch CPU-only wheel for smaller size:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==3.3.1
transformers==4.46.3
torch==2.3.1
huggingface-hub==0.27.0
scikit-learn==1.8.0
scipy==1.17.0

# Databases
neo4j==5.19.0
pymongo==4.8.0
langchain-mongodb==0.2.0

# Security
passlib==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.5.0

# Document processing
pypdf==5.2.0
python-dotenv==1.0.1

# Streamlit
streamlit==1.40.0

# Data
pandas==2.0.3
numpy==1.26.4

# Utilities
pydantic==2.9.0
tenacity==8.5.0
